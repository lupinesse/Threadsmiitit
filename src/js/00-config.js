/**
 * Application configuration.
 *
 * All tuneable constants live here. Import overrides from 00-config.local.js
 * (git-ignored) so personal settings never touch the repo.
 */

/** @type {string} Application version — kept in sync with package.json. */
export const APP_VERSION = '0.1.0';

/** @type {boolean} Enable verbose debug logging to the browser console. */
export const DEBUG = false;
