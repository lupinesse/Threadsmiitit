node_modules/
dist/

# Root-level build artefacts — generated by `npm run build`, never commit directly
script.js
styles.css

# Generated JSDoc HTML output
docs/*
!docs/qa-reports/
!docs/dead-code-reports/
!docs/screenshots/
!docs/screenshots/*.png

.DS_Store
*.local
lint-results.txt

# Generated / local-only files
test-results.txt
lint-out.json
verify-*.png
*.png
!screenshots/
!docs/screenshots/*.png

# Personal config overrides — never commit
src/js/00-config.local.js

# Local environment secrets — never commit; see .env.example for the template
.env
.env.*
!.env.example

# GitHub App private keys (e.g. chatgpt-reviewer.pem, claude-reviewer.pem) — never commit
*.pem

# Claude Code session data — ignore local state but track shared skills
.claude/*
!.claude/skills/
!.claude/token-optimization.md
